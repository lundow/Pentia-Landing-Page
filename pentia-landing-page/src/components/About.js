import React from "react";

const About = (props) => {
  return <div className="about" style={styles.container}>

  </div>;
};

let styles = {
  container: {
    backgroundColor: "#FFFFFF",
    height: "100vh",
  },
};
export default About;
