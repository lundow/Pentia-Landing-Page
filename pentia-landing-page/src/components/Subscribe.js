import React from "react";

const Subscribe = (props) => {
  return <div className="subscribe" style={styles.container}></div>;
};

let styles = {
  container: {
    backgroundColor: "#E95C55",
    height: "100vh",
  },
};
export default Subscribe;
